# Tick Box

**Tick Box** — Мини-приложение на React и TypeScript с двумя страницами:

Home Page — страница приветствия с ссылкой на меню.

Menu Page — страница с меню, где можно выбирать элементы через чекбоксы. Заголовок отображает выбранные элементы, их количество и сумму значений.

---

## 📁 Структура проекта

```
tick-box/
├─ public/                # Публичные статические файлы
├─ src/
│  ├─ assets/             # Изображения, иконки, шрифты
│  ├─ components/         # Переиспользуемые React-компоненты
│  ├─ context/            # React Context для управления состоянием
│  ├─ data/               # Файлы с данными, JSON или mock-данные
│  ├─ pages/              # Страницы приложения
│  ├─ styles/             # Глобальные и модульные стили
│  ├─ App.tsx             # Главный компонент приложения
│  └─ main.tsx            # Точка входа приложения
├─ package.json
├─ tsconfig.json
├─ vite.config.ts
└─ README.md
```

---

## ⚡ Установка и запуск

1. Клонировать репозиторий:

```bash
git clone <repository_url>
cd tick-box
```

2. Установить зависимости:

```bash
npm install
```

3. Запустить проект в режиме разработки:

```bash
npm run dev
```

4. Сборка для продакшена:

```bash
npm run build
```

5. Просмотр сборки локально:

```bash
npm run preview
```

---

## 🧩 Компоненты и структура

* **assets/** — хранение статических ресурсов (картинки, шрифты, иконки).
* **components/** — переиспользуемые компоненты React, например кнопки, чекбоксы, модальные окна.
* **context/** — React Context для глобального состояния приложения.
* **data/** — статические данные, mock API или конфигурации.
* **pages/** — компоненты страниц приложения (например, HomePage, Welcome).
* **styles/** — глобальные и модульные стили (SCSS).

---